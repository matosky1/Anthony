pipeline {
    agent {
        docker {
            image 'node:18-alpine' // lightweight and includes npm
        }
    }

    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }

        stage('Test') {
            steps {
                sh 'npm test -- --watchAll=false'
            }
        }
    }
}
